<%= form_for(@classroom) do |f| %>
  <% if @classroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classroom.errors.count, "error") %> prohibited this classroom from being saved:</h2>

      <ul>
      <% @classroom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :classroomType %><br>
    <%= f.text_field :classroomType %>
  </div>
  <div class="field">
    <%= f.label :classroomNumber %><br>
    <%= f.number_field :classroomNumber %>
  </div>
  <div class="field">
    <%= f.label :capacity %><br>
    <%= f.number_field :capacity %>
  </div>
  <div class="field">
    <%= f.label :uvaId %><br>
    <%= f.number_field :uvaId %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
