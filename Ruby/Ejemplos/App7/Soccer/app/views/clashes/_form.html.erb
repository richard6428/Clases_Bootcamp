<%= form_for(@clash) do |f| %>
  <% if @clash.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clash.errors.count, "error") %> prohibited this clash from being saved:</h2>

      <ul>
      <% @clash.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :match_id %><br>
    <%= f.number_field :match_id %>
  </div>
  <div class="field">
    <%= f.label :team_id %><br>
    <%= f.number_field :team_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
